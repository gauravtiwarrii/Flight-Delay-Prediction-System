# PostgreSQL Configuration (leave USE_SQLITE=true to use SQLite instead)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=flight_delay
POSTGRES_USER=postgres
POSTGRES_PASS=password

# Set to "false" to use PostgreSQL instead of SQLite
USE_SQLITE=true
